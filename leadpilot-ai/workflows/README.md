# Workflows

This folder stores portfolio-safe automation evidence for LeadPilot AI.

Planned files from the implementation guide include:

- `inbound-lead-qualification.REDACTED.json`
- `outbound-prospect-import.REDACTED.json`

## Publication rule

Never commit credentials, authentication headers, API keys, private webhook secrets or real confidential customer data. Before publishing an n8n export, inspect the JSON and redact sensitive values. Use `REDACTED` in the filename when the export came from a connected workflow.

The first real workflow export will be added after the relevant n8n milestone is built and tested.
